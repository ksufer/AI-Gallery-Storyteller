services:
  gallery:
    build: .
    container_name: ai-gallery-storyteller
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./db:/app/db
      - ./config:/app/config
