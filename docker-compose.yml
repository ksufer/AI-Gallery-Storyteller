services:
  gallery:
    build: .
    container_name: ai-gallery-storyteller
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env.local
    volumes:
      - ./uploads:/app/uploads
      - ./db:/app/db
      - ./config:/app/config
